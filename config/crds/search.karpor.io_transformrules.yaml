---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: transformrules.search.karpor.io
spec:
  group: search.karpor.io
  names:
    kind: TransformRule
    listKind: TransformRuleList
    plural: transformrules
    singular: transformrule
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          TransformRule is used to define the rule to transform the original resource into the desired
          target resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              type:
                description: Type is the type of transformer.
                type: string
              valueTemplate:
                description: ValueTemplate is the template of the input data to be
                  paased to the transformer
                type: string
            required:
            - type
            - valueTemplate
            type: object
        type: object
    served: true
    storage: true
